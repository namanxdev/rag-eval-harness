# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# CUAD raw corpus (40 MB); re-fetch with `python data/prepare.py --download`.
# data/eval_set.json IS committed -- it is the reproducible artifact.
data/CUADv1.json
data/raw/

# Side-experiment output from `--results <dir>` runs (model comparisons, etc).
# results/ (no suffix) is the one committed run and stays tracked.
results_*/
